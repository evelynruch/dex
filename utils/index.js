/**
 * Archivo de índice para exportar todas las utilidades
 * Facilita la importación de utilidades en los tests
 */

const TestData = require('./TestData');
const TestHelpers = require('./TestHelpers');
const BackendValidator = require('./BackendValidator');

module.exports = {
  TestData,
  TestHelpers,
  BackendValidator
};
